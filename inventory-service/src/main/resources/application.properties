# PostgresSQL
spring.datasource.url=jdbc:postgresql://localhost:5434/db_web_store_inventory_service
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=create-drop

# Avoid insert duplicates into web_store_inventory table every time the program is run

server.port = 8082